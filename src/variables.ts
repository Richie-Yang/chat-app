export enum NodeEnv {
  LOCAL = 'local',
  DEV = 'dev',
  PROD = 'prod',
  PROD_LIKE = 'prod_like',
}

export enum ImageFormat {
  JPG = 'jpg',
  PNG = 'png',
}

export enum DataType {
  BUFFER = 'buffer',
  FILE = 'file',
}

export enum DataModel {
  CHAT = 'chat',
}

export enum SchemaType {
  INPUT = 'INPUT',
  OUTPUT = 'OUTPUT',
}
